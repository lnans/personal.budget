# Optional: for production Docker builds (gitignored). Local dev uses committed `frontend/.env` instead.
# Use the URL the browser will use to reach the API (e.g. public origin or http://127.0.0.1:8080).
VITE_API_URL=http://127.0.0.1:8080
VITE_LIST_PAGE_SIZE=10
